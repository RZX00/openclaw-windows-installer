const plugin = {
  id: "workflow-zone",
  name: "Workflow Zone",
  description: "Curated workflow-zone skill pack plugin.",
  configSchema: {
    type: "object",
    additionalProperties: false,
    properties: {},
  },
  register() {},
};

export default plugin;
